<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!--
      Receipt photos live in the app's own files directory, so they are covered by the
      existing backup rules and go away with the app rather than littering the gallery.
    -->
    <files-path name="belege" path="belege/" />
</paths>
